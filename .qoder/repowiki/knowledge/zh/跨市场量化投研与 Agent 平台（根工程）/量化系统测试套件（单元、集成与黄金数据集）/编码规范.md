- 黄金场景以 JSONL 行记录，每行含 `type` 字段（bar/action/order/nav），测试侧用 `_fx_bar` 等转换函数将字典映射到 `packages.data_sources.contracts.Bar` / `CorporateAction` 等实体后再送入被测代码。
- 端到端测试通过 `pytest.fixture` 提供 `engine`（内存 SQLite）和 `ingested_repo`（经 `FakeMarketDataAdapter` + `SqlBarSink` 预灌入 ~90 日数据的仓库），下游用例只消费这些 fixture 而不重复搭建环境。
- 生产模型在测试中以 `_ProdStubModel` + `InMemoryModelRegistry` 注册，并通过 `_register_prod_model` 辅助函数完成 DRAFT→CANDIDATE→PRODUCTION 状态迁移，避免直接操作内部状态。
- 断言风格统一使用 `assert isinstance(..., Forecast|NoFill)` 配合 `pytest.approx` 校验浮点，对风险拒绝路径同时断言 `RiskVerdict.REJECT` 与 trace 中具体错误码（如 HALTED、UP_LIMIT_LOCK）。
- 时间全部使用带时区的 `datetime(..., tzinfo=timezone.utc)` 或 `date.fromisoformat`，避免 naive datetime 混入 pipeline。