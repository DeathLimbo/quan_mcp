Python ≥3.11；FastAPI + uvicorn 作为 API 入口，RQ + redis 做异步任务，SQLAlchemy2 + Alembic + psycopg3 访问 PostgreSQL，Polars/Pandas + PyArrow 处理数据，LightGBM/scikit-learn 训练，VectorBT 回测，MLflow 实验追踪，MinIO 对象存储，mcp SDK 提供 MCP Server 能力；构建使用 hatchling，工具链 ruff/black/mypy/pytest。