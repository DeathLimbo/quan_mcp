推荐 uv venv + `pip install -e ".[dev,ml,backtest,data-sources]"` 一次性安装全部可选依赖；本地依赖栈通过 `docker compose -f deploy/docker-compose.yml up -d` 拉起，随后 `alembic upgrade head` 初始化数据库，`uvicorn apps.api.main:app --reload --port 8000` 启动 API。