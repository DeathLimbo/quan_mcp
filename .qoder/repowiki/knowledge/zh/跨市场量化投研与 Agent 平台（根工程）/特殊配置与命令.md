开发环境统一通过 `uv pip install -e ".[dev,ml,backtest,data-sources]"` 安装可选依赖，再 `docker compose -f deploy/docker-compose.yml up -d` 启动基础设施，`alembic upgrade head` 执行迁移后 `uvicorn apps.api.main:app --reload --port 8000` 启动 API。