- 所有时序字段统一携带 event_time_utc / market_local_time / available_at_utc / as_of_time_utc / ingested_at_utc / calendar_version / rule_version 七元组
- 实体唯一键使用 InstrumentId(market, venue, asset_type, symbol).canonical()，禁止用 ticker 作数据库唯一键
- 训练与推理共用同一 FeatureSet.compute()，禁止两套特征实现
- 任一环节异常采用 Fail-closed 策略，风控引擎是最终授权者
- MCP 不暴露任意 SQL/Shell/Python，Skill 不修改概率/仓位数字，LLM 不承诺收益