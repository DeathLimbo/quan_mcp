# Byte-compiled / build artefacts
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
*.egg-info/
.eggs/

# Virtual envs
.venv/
venv/
env/
.env
.env.*
!.env.example

# Test / coverage
.pytest_cache/
.mypy_cache/
.pytest_tmp/

# Model artifacts (persisted by SqlModelRegistry, issue #10 Phase 1)
model_store/
.pytest_tmp/        # local scratch basetemp for sandboxed/Windows runs (issue #7)
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml

# Data / artefacts (should live in object storage, not the repo)
artifacts/
data/
raw/
mlruns/
*.parquet
*.duckdb

# IDE
.idea/
.vscode/
*.swp
.DS_Store
._*
.AppleDouble
.LSOverride
._*
.AppleDouble
.LSOverride

# Secrets / local infra
docker-compose.override.yml
*.pem
*.key
